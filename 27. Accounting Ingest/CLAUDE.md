# Project 27: Accounting Ingest

Ingests vendor invoices, bills, packing slips, quotes, and purchase orders into ProShop and QuickBooks Online. Processes email attachments and scanned documents (PDF/image) via Claude AI extraction, fuzzy-matches contacts, and uploads via GraphQL mutations. Includes scan-burst-classify pipeline, tool receiving labels, and VPO receiving automation.

## Key Files

- `accounting_ingest.py` — Main script v1.4.0: ProShopClient, QBOClient, AIExtractor (burst_pdf, classify, extract, classify_html, extract_html), email body extraction, debounced vendor search, manual label printing, cert filing by VPO
- `qbo_auth.py` — QBO OAuth 2.0 production token exchange (manual paste-URL flow via Intuit Playground redirect)
- `tool_receiving_labels.py` — Tool receiving label generator: VPO→tool library lookup (two-client), Brother PT-P700 label printing via P22 print service
- `scan_relay.py` — Watches Pictures folder, moves stable PDFs to Scanned folder
- `proshop_fields.py` — ProShop schema introspection utilities
- `diag_purchase_order.py` — Diagnostic for addPurchaseOrder mutation permissions
- `proshop_basic_auth_test.py` / `proshop_basic_auth_test2.py` — Basic auth (`/api/beginsession`) probes proving `addCustomerPo` works under user-credential flow where OAuth+`auth_010` was blocked
- `inspect_po_page.py` — Visible-mode Chrome DOM inspector for ProShop PO pages (mirrors P31's `inspect_user_page.py`). Takes `--url`, walks default + candidate sub-forms + sidebar `$formName=` links, dumps CKEditor count, file inputs, visible buttons. Outputs JSON + PNG to `logs/`. Verdict 2026-05-07 against VPO 263104: read mode has no CKEditor and no file inputs anywhere (same dead-end as user pages); attach surface is gated behind a `Checkout` button. Phase-2 (click-Checkout-then-probe) not yet implemented.

## Manual Handling reminder

Anything classified as `MANUAL_HANDLING` (vendor credit memos, debit memos, adjustment notes, chargebacks) lands in `Accounting Inbox/Manual Handling/`. The Tkinter `on_close` handler in `accounting_ingest.py` sweeps that folder when the GUI window is closed: if non-empty, a modal `messagebox.showinfo` lists the files and `os.startfile(MANUAL_HANDLING_FOLDER)` pops Explorer so Wolfgang sees the backlog before walking away. No auto-deletion — files stay until manually removed.

## Interfaces

Produces: ProShop purchase orders (addPurchaseOrder), bills (addBill), packing slips (addPackingSlip), **customer POs (addCustomerPo via basic auth — production-active as of 2026-05-13; payload expanded 2026-05-18 to include `partsOrdered` line items, `shiptoAddress`, `paymentTermsDiscount`/`paymentTermsDiscountDays`/`currency`; line items now pushed serially via `updateCustomerPo` per row to preserve PDF order — see Contracts)**, quotes (addQuote); VPO receiving (updatePurchaseOrder mutation — receivedQty/releasedQty); CustomerPo confirmationSent flipping (updateCustomerPo mutation — auto-called after each addCustomerPo); tool receiving labels (PNG via P22 print service, manual button only); QBO bills (production API, realm 123146014753554); cert PDFs filed by VPO in `Accounting Inbox/Certs/VPO-XXXXXX/` (also receives shipping labels, keyed on extracted po_number); burst PDFs in `Accounting Inbox/Scanned/burst/`; **`Outgoing Customer Confirmations/{poId} Customer PO Confirmation.pdf`** — ProShop's own confirmation form fetched via headless Chrome (URL pattern `{cpo_url}?formName=confirmation&printerfriendly=yes`) and printed to PDF after each successful addCustomerPo; **`Accounting Inbox/Manual Handling/`** — visible top-level folder for docs that need human entry in QBO (vendor credit memos, debit memos, adjustments). Auto-opens in Explorer when the GUI closes if non-empty; **`Accounting Inbox/Filed/UTILITY_NOTIFICATION/`** — non-bill utility usage notifications (HTML body + JSON sidecar); **`Accounting Inbox/Filed/PAYMENT_VOUCHER/`**; **`PROSHOP_WRITES_AUDIT.md`** — full audit (under P27 folder) of every ProShop write the ingest has made, pulled from `ingest_queue.db` (status=UPLOADED + UPLOAD_FAILED). First generated 2026-05-18 when Wolfgang surfaced that pushes have been "universally wrong"; serves as the triage doc for review/rework/delete in ProShop. Triage findings appended 2026-05-18: 11 of 15 records already deleted by Wolfgang; 4 remain (1 delete, 1 rework, 1 delete-and-redo, 1 keep) + four documented root-cause failure modes. **`[P27 ingest q#<queue_id>]` provenance tag** now prepends every ProShop write's user-visible notes field (bill `note`, PS `specialInstructions`, CPO/Quote `notes`, VPO `remarks`) so P27-pushed records are distinguishable from manual entries in the ProShop UI and the queue_id traces back to `ingest_queue.db`. `probe_addcpo_api.py` — read-only basic-auth introspection harness for `AddCustomerPoInput`/`UpdateCustomerPoPartOrderedDataInput`, twin of P35's `probe_addpo_api.py`. **`cpo_pusher.py`** (built 2026-05-20) — lightweight drag-drop / sweep-tom@-Orders CPO push tool, separate from the queue path; imports `ProShopClient`, `AIExtractor`, `download_proshop_confirmation`, and helpers from `accounting_ingest.py` as a library (the `__main__` guard makes import safe). Tk window with drag-drop (via `tkinterdnd2`), Add PDF… file dialog, Sweep button for tom@/Orders (last 30d, dedup'd via `cpo_pusher_state.json`), per-row Extract (Claude classify+extract on demand — cheap by default), Push to ProShop with `[P27 CPO drop-in]` provenance tag, dup-check modal (Open existing / Skip / Cancel / Override), and an updateCustomerPo-flips-confirmationSent background thread. **`Send Confirmation` per-row button** (session 2 2026-05-20) — after operator reviews the CPO in ProShop via the `Open in ProShop` button, clicks Send Confirmation → small modal with To (prefilled from extracted `buyer_email`, editable), subject preview, PDF name → Send → row transitions to CONFIRMED. Email goes via Graph `sendMail` from wolfgang@ with the ProShop-generated confirmation PDF attached + Wolfgang signature (HTML + inline Traxis logo). Skips email polling, ingest_queue.db, QBO side, scan/burst pipeline, and every doc type other than CUSTOMER_PO. Log tee'd to `cpo_pusher.log` for tailing outside the GUI. **`bulk_order_scatter_test.py`** (built 2026-05-20) — diagnostic harness; creates N test CPOs via bulk-add and reads back `originalSortPosition` to characterize ProShop's bulk-insert reordering. `--pad` flag toggles zero-padded itemNumber. Cleanup via `deleteCustomerPo(poId: String!): Boolean` (mutation was undocumented; probed via schema introspection 2026-05-20). **`Accounting Inbox/qbo_audit.log`** — append-only, Dropbox-synced, tab-delimited record of every QBO write attempt. Events: `BILL_CREATED` / `BILL_FAILED` / `PDF_ATTACHED` / `PDF_ATTACH_FAILED` with UTC ISO timestamp, env, vendor+id, account+id, account_source (`vendor_last_bill` or `global_fallback`), amount, doc_number, qbo_url, error.
Consumes: ProShop GraphQL API (ACCOUNTING_CLIENT_ID for purchaseorders/customerpos scope, TOOLKIOSK_CLIENT_ID for tools scope; **basic-auth path uses `ACCOUNTING_SCOPE` env var which gained `workorders:rwdp` on 2026-05-18** to support WorkOrder reads + status mutations for the back-feed mechanism), QBO REST API (OAuth 2.0 production, refresh token auto-renews — production token gets stale if env stays in sandbox mode for >100 days; `_complete_qbo_auth.py` is a one-shot helper that takes a redirect URL on argv and writes the fresh refresh token + realm to `.traxis.env`, bypassing the interactive paste prompt in `qbo_auth.py`), Microsoft Graph API (email attachments + email body text via get_body()), Claude AI API (Sonnet for extraction, Haiku for HTML classification), P22 print service (10.1.1.242:5002 /api/print-image), .traxis.env (credentials), Scanner → Pictures folder (scan_relay.py)
**Reconciliation toolkit (read-only, added 2026-05-18):** `read_proshop_invoices.py` (Stage 2 — matches ProShop invoices ↔ QBO by `invoiceId`↔`DocNumber` + total; 30-day window), `read_proshop_unbilled_shipments.py` (Stage 1 — shipped PackingSlips with blank `invoicePlainText`; 30-day window; `$` totals NOT shown because `pricePer` is null on un-invoiced slips), `read_payment_reconcile.py` (Stage 3 — QBO `Invoice.Balance` vs ProShop `Invoice.status`; 180-day window; flags `STOP_CHASING` / `PARTIAL` / `UNEXPECTED_OWED` / `AGREED` / `NOT_IN_QBO`). All three write JSON sidecars to `27. Accounting Ingest/logs/`. Invoked individually OR together via the `/reconcile` slash command at `.claude/commands/reconcile.md` (root project) which runs Stage 1+2 in parallel and formats output as a punch list referencing memories `project_proshop_qbo_zero_dollar_set1`, `project_customer_code_mapping`, `project_proshop_invoice_schema_gotchas`. **`flip_wo_invoiced_from_qbo.py`** (built + deployed 2026-05-18) — the forward mechanism for the documented one-way Web Connector gap. Detects WOs stuck on Shipped, uses ProShop's own `WO → PackingSlip.invoicePlainText → QBO DocNumber` linkage chain (NOT QBO line-description text matching — that false-positives because parts recur across years), flips to Invoiced only when QBO confirms. Dry-run by default; `--apply` to mutate; `--customer XXX` filter; `--qbo-window-days` override; `--test-telegram` for channel verification. Telegram alert (TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID env vars, same convention as P1 Overseer) fires only when an --apply run flips ≥1 WO — silent on dry-runs and zero-flip applies. **Scheduled on .71** via Windows Task Scheduler entry `Traxis - Flip WO Invoiced` (daily 18:00, RunAs TRAXIS Interactive, matches P34 model); re-deploy on srv-01 post-cutover via `setup_scheduled_flip.ps1`. ENV_PATH derived from `__file__` so it's host-portable across .178 / .71 / srv-01. `logs/proshop_wo_flip.log` — tab-delimited audit; event types `WO_STATUS_FLIP_AUTO` (scheduled run), `WO_STATUS_FLIP` (manual one-off), `WO_STATUS_ROLLBACK` (manual undo). UTC ISO timestamp, WO#, part, old/new status, matched QBO inv numbers (for AUTO).

Contracts: **All dates written to ProShop records (CPO `dateEntered` + per-line `dueDate`, WO `dueDate`, etc.) MUST be in US `M/D/YYYY` format** (no leading zeros). Confirmed empirically 2026-05-20 session 2: ProShop storage accepts both ISO and US, but downstream calculators — notably `WorkOrder.mustLeaveBy` — only parse US format. WO 26-0162 had `mustLeaveBy=None` until its dueDate was flipped from `2026-08-07` → `8/7/2026`, at which point `mustLeaveBy=8/6/2026` auto-populated within 2s. Helper: `accounting_ingest._normalize_proshop_date(value)` — handles ISO, US, dd-mon-yyyy, and natural-language inputs; returns `M/D/YYYY` or None. Old `_normalize_iso_date` retained for non-ProShop sinks. **`quantityOrdered` on `addCustomerPo`/`updateCustomerPo` displays as `N.0` on the resulting CPO line and inherited WO**, even though the GraphQL schema declares `Int` and our wire format sends pure JSON integers (verified). UI-created CPO lines display as integer `N`; API-created display as `N.0`. The Create WO button's UI validator parses the displayed string and rejects rows with `.0` suffix. Workaround until Adion fixes: operator manually retypes each qty cell back to integer in the ProShop UI before clicking Create WO. No API-side fix possible (storage IS int; display is divergent). See draft in `tom@/Purchasing - To Review` for the Joao reproducer (2026-05-20). **Confirmation email send-from-Wolfgang flow** (session 2 2026-05-20) — after CPO push + PDF gen, operator reviews in ProShop, clicks Send Confirmation in cpo_pusher, email goes via Graph `sendMail` from wolfgang@ to extracted `buyer_email` with PDF + signature + inline Traxis logo. Requires Graph app to have `Mail.Send` Application permission (granted via Entra ID 2026-05-20). SMTP `From:` is silently rewritten by Exchange from wolfgang@ → tom@ (alias→mailbox owner) but recipient-visible display name is "Traxis Manufacturing LLC" (mailbox display name set in Exchange) — customers see the brand, body signature reads "Wolfgang" (single word, no company line). Per [[feedback_no_tom_send]]: the tom@ rewrite is acceptable; brand display + Wolfgang signature carry identity. Code-side `ALLOWED_SEND_FROM = wolfgang@traxismfg.com` guard in `cpo_pusher.send_confirmation_email` refuses anything else (no-op today since Exchange overrides anyway; documents intent). Signature template at `wolfgang_signature.html`, logo at `wolfgang_signature_logo.png` referenced via `cid:traxis-logo` inline attachment. **CPO line `part` field is rev-aware** — `ProShopClient.lookup_part(client_code, identifier, rev=None)` tries `{cust}-{id}{rev}` → `{cust}-{id}-{rev}` → bare `{cust}-{id}` in order. R2Sonic-style catalogs track revs as distinct parts (`R2S1-10418A` vs `R2S1-10418B`); cpo_pusher passes the extracted `drawing_rev`/`part_rev` so the right variant routes. Falls back to bare-prefix lookup when no rev is supplied. **ProShop bulk-insert of partsOrdered does NOT preserve input array order** — confirmed empirically 2026-05-20 by `bulk_order_scatter_test.py` (5 × 20-line pushes, 5/5 identical scatter signatures). `originalSortPosition` is monotonic 0..N-1 (no gaps, no duplicates) but assigned in natural-sort order of `clientPartNumber` (or `lineItemNotes` — both had matching numeric tails in the test). Zero-padding `itemNumber` doesn't help; `itemNumber` is stored verbatim but isn't the sort key. **Workaround used by both `cpo_pusher.py` and the queue path's `addCustomerPo` (and ProShopBridge's `push_sequence_details`):** addCustomerPo with no `partsOrdered`, then one `updateCustomerPo {partsOrdered: [{data: line}]}` per line — each transaction commits before the next, so `originalSortPosition` ends up monotonic in call order. Cost: N+1 round-trips per CPO. Memory: [[project_proshop_bulk_insert_reorders]]. PO line items use `toolNumber` field (not `itemNumber`) for tools and COTS. `orderNumber` field carries brand + EDP. `shipTo` defaults to Traxis MFG 511 E St Elmo Rd. updatePurchaseOrder mutation: `id` is a separate arg (not inside `data`), poItems use selector {field: "orderNumber", value: "..."} + data {receivedQty, releasedQty, receivedDate, releasedDate}. P22 print service contract: POST /api/print-image with {image_base64, copies, label_name}. Customer PO mutations: OAuth path historically blocked at `acceptNewRecord` permission (root cause unknown); basic-auth flow (`/api/beginsession` with PROSHOP_USERNAME=full-email + PROSHOP_PASSWORD + space-delimited scope) bypasses this and works for `addCustomerPo`. Service user `auth_010` deleted 2026-05-06; AccountingConnector OAuth client is now mapped to nothing/undefined. **Customer PO writes now go through inline `_BasicAuthSession`** (`add_customer_po`, `update_customer_po`) — beginsession → query-param token → auto-refresh on 401. Every other ProShop mutation stays on OAuth. Mirrors P35's `purchasing/proshop_basic_auth.py`; kept inline to avoid cross-project Python import. ProShop audit log shows Wolfgang's name as creator for these mutations, not a service user. **QBO Bill account categorization mirrors the QBO UI**: `create_bill` resolves the line-item AccountRef by querying the vendor's most recent Bill (`SELECT * FROM Bill WHERE VendorRef = ? ORDERBY TxnDate DESC MAXRESULTS 1`) and reusing line[0]'s account; falls back to first COGS, then first Expense, if the vendor has no prior Bills. Source recorded in qbo_audit.log per push. **ProShop dup-check** uses per-doc-type filter field (`bills.referenceNumber`, `customerPOs.clientPONumber`, `purchaseOrders.confirmationNumber`, `packingSlips.referenceNumber`, `quotes.referenceNumber`); normalizes leading `PO`/`P.O.`/`PO#`/`INV` prefix; parses dates loose (ISO or `M/D/YYYY`); 6-month window. On match: hard refuse — no override — row marked `POSSIBLE_DUPLICATE` with `proshop_url` pointing at the existing record. **Permanent reject**: `check_local_duplicate` prefers REJECTED matches over any other; re-ingested content matching a previously-rejected `doc_hash` auto-rejects on insert (applies to email PDF, email body, and scan paths). **CPO confirmation auto-generation**: after successful `addCustomerPo`, background thread runs `download_proshop_confirmation()` — headless Chrome logs in, navigates to `{proshop_url}?formName=confirmation&printerfriendly=yes`, prints to PDF via Chrome DevTools `Page.printToPDF`, saves to `Outgoing Customer Confirmations/`. Then `updateCustomerPo` flips `confirmationSent=true` + `confirmationSentBy` + `confirmationNotes=<file path>` on the ProShop record. **`MANUAL_HANDLING` doc type** files to `Accounting Inbox/Manual Handling/` (top-level, visible). The Tkinter `on_close` handler pops a modal + opens Explorer if the folder is non-empty — never auto-deletes. **ProShop Invoice number is `Invoice.invoiceId`** (not `legacyId`, no `invoicePlainText` on Invoice itself). **`Invoice.status` is effectively unused** — binary Outstanding/Booked with only 2 Booked across 1378 invoices over 5 years; the real "is this invoiced" status lives on **`WorkOrder.status`** (values: Active, Manufacturing Complete, Shipped, Invoiced, Complete, Canceled). **`ProShop Contact.accountingGuid`** is the link field for QBO sync — despite the "GUID" label it takes the **QBO Customer DisplayName** (e.g. `OPMobility`), NOT the numeric Id. Set this on the Contact record to clear "Bill Sync Error" banners on that customer's invoices. **`updateWorkOrder` mutation confirmed working under basic auth 2026-05-18** (probed on OPM1 26-0155 + batch of 11 stuck WOs). **ProShop→QBO sync is create-only** — once an invoice exists in QBO, ProShop-side edits do NOT propagate; must repeat in QBO manually or Stage 2 reconcile will flag as amount mismatch.

## Next Steps

- ~~[NEEDS WOLFGANG] Review + send Joao bulk-insert ordering draft~~ **SENT** between sessions on 2026-05-20.
- **[NEEDS WOLFGANG] Review + send the second Joao email draft** (added 2026-05-20 s2). Short note re: `quantityOrdered` displaying as `N.0` on API-created CPO lines (Adion-side UI/storage divergence; their Create WO validator parses display string and chokes on `.0`). Also mentions the `mustLeaveBy` requires-US-date-format quirk as FYI (we fixed our side). Sitting in `tom@/Purchasing - To Review`. Open in Outlook, set From=wolfgang@ before sending.
- **[NEEDS WOLFGANG] Manually retype each qty cell on R2S1-PO115267 lines 02-16 before Create WO** (added 2026-05-20 s2). Until Adion fixes the display formatter, every API-created CPO line shows qty as `N.0` and the Create WO button refuses. Manual UI retype (e.g., backspace + retype `20` over `20.0`, Tab off) re-stores the cell as integer and unlocks Create WO. Line 01 already done; remaining 15 product lines + 8 conformance cert lines (which show `1.0`) all need this. Painful but one-shot.
- **[URGENT] OAuth scope mismatch breaks production read paths** (surfaced 2026-05-20). `ACCOUNTING_SCOPE` in `.traxis.env` includes `workorders:rwdp` (added 2026-05-18 for the WO back-feed), but the OAuth client at Adion doesn't allow it. Token endpoint returns `403 invalid_scope` — every `proshop.query()` call in `accounting_ingest.py` fails: dup-check (`_check_proshop_duplicate`), `get_contacts` (fuzzy contact match), packing-slip lookups, etc. Confirmed broken on .178; not yet verified on .71. Two fixes: (a) ask Joao to add `workorders:rwdp` to the OAuth client allowed-scopes — quickest if production still runs the OAuth read paths anywhere; or (b) migrate `accounting_ingest.py`'s reads to basic auth (same pattern `cpo_pusher.py` now uses for `fetch_contacts_via_basic` + `check_proshop_cpo_duplicate`). Path (b) also frees us from needing OAuth at all on this project.
- **Apply the serial-update fix to `accounting_ingest.py:2061 _build_cpo_items`** (added 2026-05-20). Same bulk-insert ordering bug `cpo_pusher.py` just fixed — every queue-path CPO pushed since 5/13 has lines in arbitrary order (natural-sort-by-clientPartNumber). Either patch `ProShopUploader._upload_customer_po` to do empty-header create + per-line `updateCustomerPo`, or accept the bug if Wolfgang's plan is to retire the queue path's CPO route in favor of `cpo_pusher.py`.
- ~~[NEXT SESSION] Wire `cpo_pusher` confirmation PDF into an Outlook outgoing-confirmation flow~~ **DONE 2026-05-20 s2** as a programmatic `sendMail` flow (Mail.Send granted; Exchange rewrites SMTP From to tom@ but brand display reads "Traxis Manufacturing LLC" + body signs as Wolfgang per the clarified [[feedback_no_tom_send]] rule). Per-row **Send Confirmation** button + modal → Graph sendMail from wolfgang@ with PDF + signature + inline logo → CONFIRMED status. `tom@/Confirmations - Outgoing` folder created as the fallback-draft sink (rarely used now).
- **[NEEDS WOLFGANG] Verify first scheduled run** (added 2026-05-18 s5). The `Traxis - Flip WO Invoiced` task on .71 runs tonight at 18:00. Tomorrow morning, check `logs/proshop_wo_flip.log` to confirm it ran (look for a 2026-05-18T~22:00Z or 2026-05-19T entry from .71). Probably silent — we cleared all 12 stuck WOs today. If anything flipped, you'll have gotten a Telegram ping at 18:00ish.
- **[Cutover] Re-deploy WO flip task on srv-01 post-cutover** (added 2026-05-18 s5). When srv-01 cuts over, run `setup_scheduled_flip.ps1` there (the .ps1 is in this folder, host-portable). Required prereqs on srv-01: `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` set as User OR Machine env vars; Python interpreter at the path passed via `-PythonExe` (the WindowsApps proxy default works if Python was installed via Store-style installer, otherwise pass an explicit path). Then `Unregister-ScheduledTask -TaskName "Traxis - Flip WO Invoiced"` on .71 to retire the old one. Same note as the broader srv-01 cutover; just adding this one task to the cutover checklist.
- **Spot-check the 12 WOs flipped today** (added 2026-05-18, refined s5). Original 11 batch + 1 probe + 4 forward-mechanism = 12 WOs flipped Shipped→Invoiced today: 26-0007, 26-0130, 26-0150, 26-0151, 26-0154, 26-0146, 26-0147, 26-0155 (from s3 batch + probe), 25-0200, 25-0375, 26-0081, 26-0142 (from s5 forward-mechanism re-flip after morning rollback). All 4 from the forward-mechanism path have definitive ProShop→QBO linkage (logged in `logs/proshop_wo_flip.log` with the QBO inv numbers). The 7 batch ones could use a casual eyeball, especially 26-0154 (shipped 5/12 but matched QBO inv from 3/10 — wide date gap looks suspicious; might be a renumbered invoice).
- **[NEEDS WOLFGANG] Clean up 4 remaining ingest records in ProShop** (triage now in `PROSHOP_WRITES_AUDIT.md` as of 2026-05-18). The other 11 records Wolfgang already deleted. Remaining actions: **DELETE `260410001`** (bill in unused module per [[project_proshop_bills_module_unused]]); **REWORK `263085`** (Water Cut VPO — attach correct supplier contact, or delete + redo); **DELETE `263104`** (R2Sonic CPO mis-pushed as VPO, supplier=TRA1 wrong); **KEEP/VERIFY `263089`** (Bayou Metal aluminum VPO — record looks correct, just was queue-labeled as quote). Triage section lists the four documented root-cause failure modes (extraction customer/vendor confusion, doc-type-without-route fix landed 2026-05-18, `addBill` field-shape silently null body, contact picker accepts no-match).
- **[NEEDS WOLFGANG] Restart P27 GUI to pick up doc-type dropdown fix** (added 2026-05-18). New `<<ComboboxSelected>>` binding on the Type pulldown now updates `_current_doc_type` + flips the contact panel header + re-runs the contact search when you reclassify. Verify by picking "Customer PO → ProShop" on a row originally classified as something else, then typing the customer name — the listbox should show ProShop contact matches, not QBO vendors.
- **Remove ProShop bills push path** (added 2026-05-18). Wolfgang: "we don't use proshops bills module" — confirmed via writes audit (bill 260410001 in an unused module). Delete `ProShopUploader._upload_bill`, `ProShopClient.add_bill`, the `_build_bill_items` helper, and the `VENDOR_INVOICE → addBill` branch in `ProShopUploader.upload()`. `VENDOR_INVOICE` should route to QBO only. See [[project_proshop_bills_module_unused]] in memory for rule rationale.
- **[NEEDS WOLFGANG] First Bill push to QBO + audit-log validation.** 34 May emails are PENDING (mostly VENDOR_INVOICE). Approve one through to QBO. Verify `Accounting Inbox/qbo_audit.log` gets a `BILL_CREATED` line; check `account_source=vendor_last_bill` is winning over `global_fallback` for established vendors (Hadco, TCI Precision, McMaster). Global_fallback rows need a one-time category fix in QBO after posting — subsequent Bills from the same vendor follow the fix automatically.
- **[NEEDS WOLFGANG] Test the on-close Manual Handling popup.** When you classify your first vendor credit memo as `MANUAL_HANDLING` and approve, the PDF/HTML lands in `Accounting Inbox/Manual Handling/`. Close the GUI window — expect a modal listing the items + Explorer popping to the folder. Confirm the dialog text "odd ducks to deal with" is what you wanted; tweak if not.
- **[NEEDS WOLFGANG]** Send drafted email to Joao @ Adion (text in `joao_reply_2026-05-06.txt`). Use Outlook with From = wolfgang@traxismfg.com.
- **[NEEDS WOLFGANG] Provide a real customer PO URL** for Phase-2 inspect. Now that `R2S1-PO115245` exists from this session's live push, the inspector can target it directly — but the question is whether CPO pages expose an attach surface that VPO pages don't. Run `python inspect_po_page.py --url https://traxismfg.adionsystems.com/procnc/customerpo/2026/R2S1-PO115245` whenever convenient.
- **[Deferred until post-server-install] GUI rewrite from Tkinter to Flask/HTML.** Tkinter ceiling is real — `ttk.Style` polish only gets ~60% there. Real fix is a Flask localhost UI reusing the bridge's `palette.html` token set; same model P31 uses. Same `ingest_queue.db`, same `QBOClient`, same `qbo_audit.log` — just swap the presentation layer. Hold until OptiPlex Micro consolidates Flask deployment.
- **[Deferred] Structured utility-metering capture (option C).** `UTILITY_NOTIFICATION` files HTML body + JSON sidecar today. Future: add a Claude extraction prompt to pull `kwh_used`, `peak_demand`, `period_start`, `period_end`, `cost_to_date`, `vs_prior_period_percent` into a `utility_metrics.db`. Hook P32 Breakeven Dashboard to read it for $/kWh against machine runtime.
- **Phase-2 PO-page inspector — click Checkout first, then probe.** 2026-05-07 inspect of VPO 263104 found no CKEditor and no file inputs in read-mode. The 4 visible buttons suggest the attach surface is gated behind `Checkout`. Extend `inspect_po_page.py` to drive the Checkout (and `+`) buttons, wait for settle, re-scan. Run on both VPO and CPO pages — likely different. Outcome decides whether attachments to PO pages are possible via Selenium (rasterized PDF → CKEditor, or native file widget).
- **Fix `_poll_now` button bug** — `App._poll_now` calls `self._email_poller.run()` which spawns the background loop on every click. Each click adds another loop. Two-line fix: switch to `threading.Thread(target=self._email_poller.poll_once, args=(10,), daemon=True).start()` (uses the one-shot method already added). The Backfill button uses `poll_once` correctly today.
- **Test the other heavy mutations under basic auth before migrating the rest of the pipeline.** Customer PO is proven (live 2026-05-13). **`updateWorkOrder` now confirmed working under basic auth 2026-05-18** (used to flip 12 stuck WOs, 4 of which got rolled back via the same mutation). Still to confirm under basic auth: `addPurchaseOrder`, `addBill` (ProShop side, not QBO), `addPackingSlip`, equipment mutations, `updatePurchaseOrder`, `updateInvoice`. Only after these confirm no functional gaps should we route them through `_BasicAuthSession` too — until then, OAuth stays load-bearing for everything except `addCustomerPo`/`updateCustomerPo`/`updateWorkOrder`.
- ~~Wire `part` (Traxis internal part#) on CPO line items.~~ **DONE 2026-05-19** (commit 5dd3832). Both `accounting_ingest.py` and `cpo_pusher.py` resolve `{client_code}-{drawing_or_part_number}` via `parts(filter:{partNumber})` before push.
- **Surface `paymentTerms` / `taxStatus` / `fob` enum values somehow.** Introspection of `CustomerPOPaymentTerms`, `CustomerPOTaxStatus`, `CustomerPOFOB` returns null at our scope — currently we pass-through whatever extraction produced ("Net 30", etc.) and ProShop silently drops mismatches. Either learn legal values by reading existing CPOs (also locked at our scope — `customerPOs(pageSize:N)` returns 0 records — Wolfgang may need to ask Joao), or build an explicit map of common strings → likely enum names.
- ~~[Deferred — pending audit triage] Lightweight CPO drop-in tool.~~ **DONE 2026-05-20** as `cpo_pusher.py` (~530 lines). Drag-drop via `tkinterdnd2`, sweep tom@/Orders, CPO-only, serial-update line push, dup-check modal, auto confirmation PDF + `confirmationSent` flip. First live push (R2S1-PO115245) succeeded. Remaining scope from the original spec — VPO/PS/Quote support, watched-folder mode — deferred; CPO accuracy was the bottleneck and is now resolved.
- **[Deferred — planning only] S/MIME for Traxis humans** (discussed 2026-05-20 s2). Wolfgang asked the broader rollout question after confirming the automated confirmation flow stays unsigned (option 1). Roadmap: (a) buy per-user S/MIME certs from Sectigo (~$20-40/yr) or DigiCert (~$60-90/yr); (b) verify with the CA's email-validation flow; (c) install .pfx on each user's primary Outlook desktop and configure Trust Center → Email Security → Default Settings; (d) check tenant SKU — if on M365 Business Premium or E3/E5 with Intune, can do cloud-managed S/MIME via SCEP instead of BYO-cert. Practical constraint: OWA and mobile have limited/no S/MIME support, so people would only sign from primary workstation; phone replies break the signed thread. Sequence to recommend: tighten DMARC first (run mxtoolbox check on traxismfg.com), then 1 cert for Wolfgang as pilot, then expand to Tom + anyone else who sends customer-facing mail. Don't sign automated tool sends with personal certs (anti-pattern; if anything, use a dedicated automation cert).
- **[Deferred — planning only] QBO bill-routing pivot.** Wolfgang 2026-05-18: "QBO has a flawless document ai. Route bills and invoices to a queue for QBO with a de-dup check." Recommended mechanism (from this session's research): forward bill/invoice PDFs to QBO's `@qbodocs.com` receipt-capture address; QBO's own AI extracts and lands them in the Receipts tab for review. Eliminates our bill-side AI extraction entirely. Email source: tom@/Orders (Wolfgang now manually sorts incoming mail into Outlook folders). Pure planning today; pick up after the accuracy audit clarifies whether the ProShop side is salvageable. Don't touch `accounting_ingest.py` for this without an explicit go.
- **Watch for Joao's reply** on (a) whether any API operations are OAuth-only, (b) whether `acceptNewRecord` control gets documented. Determines whether we eventually retire OAuth entirely or keep it for specific operations.
- **If OAuth is kept around**, set up a fresh service user once `acceptNewRecord` control is documented. Don't repeat the auth_010 dead-end.
